import { defineStore } from 'pinia'
import { base_URL } from './Url'
import axios from 'axios'
import Swal from 'sweetalert2'

export const useDataCategory = defineStore('category', {
    state: () => ({
    }),
    getters: {
  
    },
    actions: {
        
    }
  })
  